plugins {
    id 'java-library'
}

dependencies {
    implementation configurations.common,
            configurations.saml_lib,
            project(':hub-saml')

    api configurations.test_utils,
            configurations.saml_test
}
